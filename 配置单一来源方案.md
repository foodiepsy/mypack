# 配置单一来源（Single Source of Truth）重构方案
> 状态：**方案待确认，尚未实施任何代码改动。**
> 对应 DEEPSEEK_REVIEW_2_IMPROVEMENTS.md 改进项 #4。
> 生成方式：DeepSeek agent 自主读源码分析 + 人工逐条核验（下表所有行号/数值均已人工确认）。

## 一、问题现状
同一个物理量在仓库里被写了 2~3 份，彼此之间没有引用关系，只能靠"记得同步改"来保证一致。
### 1.1 电芯热物性 / 几何
**最危险的一条**：CellThermalModel(Lx, Ly, Lz) 不传 k 就会拿到各向同性 1.5 W/(m·K)，而 314Ah 电芯真值是各向异性 (12, 0.7, 11.6)。这不会报错，只会**静默算出一个物理上错误的温度场**——Y 向（厚度）导热被高估 2 倍、X/Z 向被低估近 8 倍，堆叠方向的温度梯度会被完全抹平。
### 1.2 场景配置（DeepSeek 未覆盖，人工补充发现）
demo1-8s-foam/demo_8s_foam_thermal.py 与 deepseek_agent/tools.py:run_8s_foam **逐字重复**了同一套 8S 泡棉场景常量：
**风险**：agent 的 run_8s_foam 存在的意义就是"复现 demo1 的基准结果"（peak 308.42 K / ΔT 2.388 K / Vt 17.62 V @0.5C）。一旦有人调 demo1 而忘了同步 tools.py，agent 会**继续自信地报一个已经和基准脱钩的数**，且没有任何机制能发现。这比 §1.1 的 k=1.5 更隐蔽，因为两边各自都能跑通。

## 二、三个候选方案
### 方案 A：defaults.py 为权威单源，下游类删除数值默认（全部必填）
CellThermalModel / StackThermal3D 构造函数移除所有物性默认值，改为必填；调用方一律从 defaults.py 取。
### 方案 B：抽 frozen dataclass 配置对象（CellConfig / PackScenario）
    CellConfig(frozen)   ← 几何 + 热物性 + 电气，defaults.py 只导出 DEFAULT_314AH 单例
    PackScenario(frozen) ← 泡棉/塑料/对流/网格/截止电压，导出 SCENARIO_8S_FOAM 单例

ECMCellSpec / CellThermalModel / StackThermal3D 增加 from_config(cfg) 类方法；demo1 与 tools.py 都改为 StackThermal3D.from_config(DEFAULT_314AH, SCENARIO_8S_FOAM)。
### 方案 C：下游类默认值改为**引用** defaults.py（而非复制）
    # thermal3d.py
    def __init__(self, Lx, Ly, Lz, ..., rho=None, cp=None, k=None, h=None, ...):
        rho = defaults.RHO_DEFAULT if rho is None else rho
        k   = defaults.K_DEFAULT   if k   is None else k
        ...


## 三、推荐路线：C → B 两阶段
单选一个都不理想——C 见效快但治标，B 彻底但一次性改动太大、违背"别干崴了"的原则。建议分两步：
### 阶段 1（低风险，建议立即做）
thermal3d.py:78-79 的 rho/cp/k 默认值改为哨兵 None + 从 defaults 取值；**重点是干掉 **k=1.5。
thermal3d.py:126 _parse_h 里写死的 h.get("default", 5.0) 改为引用 defaults.H_DEFAULT（新增该常量，值仍取 5.0，保证行为不变）。
defaults.py 增补 T_AMB_DEFAULT = 298.15，thermal3d.py / thermal3d_stack.py 的 298.15 全部改为引用。
回归验证：全量 pytest（当前 61 passed）+ demo1 基准三数不变。
> ⚠️ 阶段 1 第 1 步会**改变** CellThermalModel(Lx,Ly,Lz) 不传 k 时的行为（1.5 → (12,0.7,11.6)）。**已做过一次性 dry-run 验证**：临时把 thermal3d.py:78 的 k=1.5 直接改成 k=(12.0, 0.7, 11.6) 后跑全量 pytest，结果 **61 passed，零失败**，改动已回滚。原因：tests/test_thermal3d.py 共 22 处 CellThermalModel(...) 调用，其中 14 处显式传了 k=1.5（不受影响），剩余 8 处不传 k 的调用全部是 **构造器校验类**（h 参数解析、R_shell 串联热阻）或 **能量守恒/单调性类**断言，都不依赖 k 的具体数值。结论：这一步**没有回归风险**，可以放心做。
### 阶段 2（结构性，建议单独一次提交）
新增 ecm_pack/config.py：CellConfig / PackScenario 两个 frozen dataclass，defaults.py 导出 DEFAULT_314AH / SCENARIO_8S_FOAM 单例。
StackThermal3D.from_config(cell_cfg, scenario, n_cells) 类方法（原 __init__ 保留，不动，保证兼容）。
demo1 与 deepseek_agent/tools.py:run_8s_foam 双双改为走 from_config，**消除 §1.2 的逐字重复**。
新增回归测试 test_config_single_source.py：断言 from_config 构造出的 StackThermal3D 与当前 demo1 手工构造的对象，在 _A/_C/_bc_rhs 上逐元素相等（容差 1e-12）——这是"重构零行为变化"的硬证据。
### 验收标准（两阶段共用）
pytest tests/ -q 全绿（当前基线 **61 passed**）
demo1 @0.5C 三数一字不差：**peak 308.42 K / ΔT 2.388 K / Vt 17.62 V**
deepseek_agent 的 run_8s_foam(0.5) 输出与 demo1 一致

## 四、需要你拍板的三件事
**是否按"C → B 两阶段"走**，还是只做阶段 1 就收手？
**阶段 1 第 1 步改变 **k 默认值（1.5 → 各向异性）算 bug fix 还是 breaking change？如果这个库已有外部使用者，需要在 README 写迁移说明。
**阶段 2 的 **PackScenario 边界在哪——只装热学场景（泡棉/塑料/对流/网格），还是把电气侧（v_cut_lower、dt、record_every、8S1P 拓扑）也一起装进去？我倾向前者，把仿真控制参数留在调用方，否则 config 会膨胀成"什么都装"的上帝对象。

**确认后我再动代码。当前仓库未做任何 #4 相关改动。**

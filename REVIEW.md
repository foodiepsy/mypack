# mypack 代码评审（2026-07-28）

> 评审对象：`foodiepsy/mypack`（`main` 分支，截至 `c6ffc2b`）
> 评审方式：静态通读 + 针对数值结论的脚本复算验证。每条"缺陷"均给出**可复现证据**。
> 范围：最近一轮工作——① `StackThermal3D.plot_3d_pack` 真 3D 渲染；② demo1 8s-foam 仿真物理；③ `demo1-6`+`demo_old` 重组结构；④ 遗留问题。
> 不重复覆盖 `90a154e` 已修的扩散边界/功率控制/R=0 三处缺陷（那一版 REVIEW.md 本身已损坏，见附录 D）。

---

## 一、总体评价

结构分层清晰、3D 渲染满足"一眼看清 8 个体块+泡棉/空气+顶塑料+底条件"的需求。**但发现 1 个高严重度物理 bug（Arrhenius 温度符号反了）**，直接导致 demo1 在 SoC≈0.23 虚假提前截止；另有一处注释自相矛盾、测试覆盖缺口与两处历史编码损坏。

---

## 二、发现清单（按严重度）

### ? 高：`R0_314ah` / `R1_314ah` 的 Arrhenius 温度依赖符号反了（核心物理 bug）

- **位置**：`ecm_pack/defaults.py` 第 51 行（`R0_314ah`）、第 58 行（`R1_314ah`）
- **现象**：电阻随温度升高而**增大**。

  | 工况 | R0（当前模型） | 物理应有 |
  |---|---|---|
  | 25°C | 0.523 mΩ | 0.523 mΩ |
  | 74°C | **1.709 mΩ**（↑3.27×） | **0.160 mΩ**（↓） |

- **物理**：电池内阻应随温升而**下降**（离子/电子电导率↑），标准形式为 `R ∝ exp(+Ea·(1/T ? 1/T_ref))`，Ea>0 时 T 越高 R 越小。代码写成 `exp(2500·(1/T_ref ? 1/T))`，即温度差**符号反了**。
- **后果（脚本复算证据）**：

  ```
  SoC=0.233, T=74°C  ->  V_terminal = 2.035 V   (当前模型，踩中 v_cut_lower=2.0V)
  SoC=0.233, T=74°C  ->  V_terminal = 2.517 V   (正确符号，应继续放电)
  ```

  - demo1 的 `Pack.solve` 在任意活性电芯 `V<2.0V` 时 `break`（`pack.py:308`）。当前模型在 ~46min、SoC≈0.23 处端电压被虚高内阻拉到 2.0V 而终止。
  - 用正确符号，同点端电压 2.517V，放电本应继续到远低于 0.23 的 SoC，接近完整的 1C/2h 放电。
  - **即：demo1 当前的 3D 温度场与所有曲线只代表被截断的 ~46min 放电，并非设计目标的 2 小时**；峰值 ~347K 也受此双重影响（高 R 既增热又促截止）。
- **修复建议（一行级）**：把 `2500.0 * (1.0/298.15 - 1.0/(Tdeg+273.15))` 改为 `-2500.0 * (...)`（或交换相减顺序）；`R1_314ah` 中的 `2000.0` 同样处理。
- **影响面**：`defaults.py` 是全库所有仿真的参数源，改动影响全局。建议先与用户确认参数口径再改（符合"别干崴了"）。

### ? 中：demo1 报告注释与物理/数值自相矛盾

- **位置**：`ecm_pack/examples/demo1-8s-foam/demo_8s_foam_thermal.py` 第 143 行
- **原文**：`bat8-bat1={...:+.3f}K（顶部塑料片热阻高->bat1偏凉）`
- **问题**：
  1. 打印值 bat1=336.16K 比 bat8=335.86K **高** +0.30K，即 bat1 实际**更热**；
  2. 顶部塑料是**隔热**层，本应使 bat1 更热而非更凉。
- 注释因果写反。建议改为：`bat8(底部空气对流)最凉，bat1(顶部塑料隔热)略高于bat8，中间电芯(仅薄侧散热)最热`。

### ? 低：`plot_3d_pack` 无单元测试覆盖

- `tests/test_thermal3d_stack.py` 仅有 3 个测试（能量守恒 / 泡棉方向 / 逐芯温度），未覆盖 `plot_3d_pack`。
- 该渲染含较多 3D 几何索引（`block_faces` 的 `xi/a/b` 越界风险、`zoom` 因子、颜色映射）。建议补一个 smoke test：构造 `StackThermal3D` → `step` 几步 → 调 `plot_3d_pack(save_path=...)`，断言返回 `(fig, ax)` 且 PNG 生成。

### ? 低：demo3 / demo4 中文注释损坏（不可从 git 恢复）

- 此前为修 `SyntaxError` 用 `errors='replace'` 重编码，把中文变成 `?`（如 `# demo_314ah_3d.py —?  314Ah 大电? + 多维热模?`）。
- 这两个文件在 git 只有 `c6ffc2b` 一个提交，干净中文已丢失；只能按脚本逻辑重新手打注释。逻辑未损，优先级低。

### ? 低：已存在的 `REVIEW.md` 本身是乱码坏文件

- 来自 `90a154e`，GBK 字节被当 UTF-8 解码成乱码（`浠ｇ爜` = "代码" 损坏态）。本次评审已用本文件覆盖重写。

### ? 通过项

- **结构重组**：6 个 demo 经 `git mv` 迁移，`sys.path` 全部 4 层、产物落各自 `result/`、旧脚本+图归档到 `demo_old/`；`pytest tests/` = **54 passed, 6 warnings**（警告为 `thermal3d.py:459` 的 CJK 字体缺字，历史遗留、与本轮无关）。
- **demo1 物理边界**：底部 `h_bottom=50` 空气对流（非绝热）?；泡棉仅 +X/+Z、?X/?Z 直空气 ?；顶部塑料薄层 ?。
- **快照对齐**：`thermal.T = T_end.copy()` 使 3D 渲染与报告末态一致，消除 ~2.4K 错位 ?。
- **3D 渲染质量**：8 个体块按完整 3D `T_field` 逐面着色 → X 轴梯度（泡棉面更热 / 直空气面更凉）可见 ?；泡棉橙、顶部塑料青、底部空气蓝+对流箭头、绝热回退斜纹 ?；2D BC 摘要框 + 色标 + 图例齐全 ?。

---

## 三、对 3D 渲染可视化的两点改进建议（可选，非 bug）

1. 因 Y 轴被 8 芯拉长（y_bot≈0.57m vs Lx=0.17m），X/Z 面在图上偏窄，X 轴梯度虽存在但视觉不突出。可在图角加一个 X–Z 切面 inset，或把体块的 X 面单独放大显示。
2. 当前每体块 6 面各细分 10×10=600 片 + 泡棉/边界，8 芯约 4000+ `Poly3DCollection`，单图渲染偏重但可接受（一次性出图）。

---

## 四、结论与建议动作（待拍板，符合"别干崴了"）

| 优先级 | 动作 | 风险 | 建议 |
|---|---|---|---|
| ? 最高 | 确认/修复 `R0_314ah`/`R1_314ah` 的 Arrhenius 温度符号 | 影响全库 | 建议修；改前先确认参数口径，再重跑 demo1 |
| ? 中 | 修订 demo1 第 143 行注释 | 极低 | 可直接改 |
| ? 低 | 补 `plot_3d_pack` smoke test | 低 | 建议补 |
| ? 低 | 重写 `REVIEW.md` | 无（覆盖坏文件） | 本次已做 |
| ? 低 | 重打 demo3/demo4 中文注释 | 低 | 按需 |

> **核心判断**：结构重组与目标可视化均已达标；**真正需要你拍板的是 Arrhenius 符号**——它既是一个物理错误，也是 demo1"2 小时放电"无法跑满的根因。修完后 demo1 的温升与 3D 场会显著不同，值得重跑确认。

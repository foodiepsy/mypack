# ecm_pack 拓扑热切换（可重构电池包）实现说明

> 用户需求："我需要它可以在特定时间或随时进行拓扑切换，比如一组 8S 在工作 10min 之后另一组 8S 并入工作，可以做到吗？"
> **结论：可以，且已实现、已验证。**

---

## 1. 核心设计思想

拓扑切换之所以能"热"进行，关键在于**把电芯的时序状态（SoC / RC 电压 / 温度）与电路网表彻底解耦**：

- 每个电芯对象 `ECMCell` 永久持有自己的状态，无论是否接入电路都不丢失；
- 网表（MNA 节点图）只是"当前这一刻谁和谁连、连成什么拓扑"的快照；
- **切换拓扑 = 替换网表 + 替换"接入映射 `active`"**，不触碰任何电芯的 SoC/RC/温度。

由此得到三个天然性质：

| 性质 | 表现 |
|------|------|
| **状态保留** | 被断开的 group B 在待机期间 SoC/RC/T 完全冻结，并入时按自身状态立即参与 |
| **自动浪涌/环流** | 新并入的高电压组与低电压组在并联瞬间，由 MNA 自动解出组间环流（无需任何特殊处理） |
| **任意重构** | 定时切换、事件驱动（工况）切换、手动随时切换，三种入口全部支持 |

---

## 2. 对外 API

### 2.1 构造时指定初始激活集合
```python
pack = ep.Pack(cells, netlist_solo, thermal=th, active=act_solo)
```
`active` 是"网表第 k 个 V 元素 ↔ cells 中第几个电芯"的映射，顺序必须与网表 V 元素顺序一致。

### 2.2 求解时挂接定时/回调切换
```python
out = pack.solve(
    dt, control, control_type="current", n_steps=1200, record_every=5,
    # 定时：到达 t_switch 自动把 (网表, active) 换上
    topology_events=[(600.0, netlist_par, act_par)],
    # 事件驱动：每步回调，返回 (网表, active) 即切换（按工况/随时）
    switch_callback=lambda t, p: (netlist_par, act_par) if t >= 600 else None,
)
```

### 2.3 运行中随时手动切换
```python
pack.set_topology(netlist_par, act_par)   # 立即生效，电芯状态零丢失
```

### 2.4 双组拓扑工厂（演示用）
```python
nl_solo, act_solo, nl_par, act_par = ep.setup_two_group(nS=8)
```
返回「单组 8S」与「两组 8S 并联（8S2P）」两套网表及各自 `active`，节点布局相同（负极 node0、正极 node8），因此两组在电气上天然并联跨接整包两端。

---

## 3. 验证场景与结果

**场景**：16 芯（group A = cell 0..7，group B = cell 8..15）。group A 以 8A 先放 10min（SoC 跌到 ~0.535），group B 待机于满电 1.0；t=600s 时 group B 并入，两组 8S2P 并联共担 8A 负载。

| 指标 | 切换瞬间 (t=600s) | 末态 (t=1200s) | 说明 |
|------|------------------|----------------|------|
| A0 SoC | 0.5350 | **0.5943** ↑ | 被 B 组充电，SoC 回升 |
| B0 SoC | 0.9983 | **0.6724** ↓ | 向 A 组放电 |
| SoC 差 Δ | 0.4634 | **0.0781** | 10min 内基本均衡完成 |
| A0 电流 | **-21.81 A** | 2.44 A | 负号 = 被充电（浪涌） |
| B0 电流 | **+29.81 A** | 5.56 A | 猛放电灌入 A 组 |
| 整包电流 | 8.00 A | 8.00 A | 负载恒定 |
| 端口电压 | 31.607 V | 31.607 V | 恒流下并联组合电压稳定 |

**物理判读**：
- 切换瞬间 B 组（满电、高电压）向 A 组（半电、低电压）灌入约 **21.8A 环流**，同时两组共同承担 8A 负载 → 表现为 B0 电流 ≈ 29.8A、A0 电流 ≈ -21.8A。这完全由 MNA 在"两组并联"的网表结构上自动解出，证明耦合器正确。
- 随后 10min，两组 SoC 差从 0.463 收敛到 0.078，整包等效容量/电流能力随第二组的并入而翻倍——正是可重构电池包想要的效果。

图表：`/workspace/ecm_pack_topology_demo.png`

---

## 4. 实现中修复的关键坑（供复用时避坑）

1. **MNA 奇异矩阵（节点空洞）**：`setup_two_group` 最初把私有节点编到 `1000+` 号，导致串级节点 (0..8) 与私有节点 (1016..1023) 之间出现大量**完全无连接的孤立节点**，MNA 矩阵奇异。改为私有节点紧贴串级节点之后**连续编号**（单组 nS+1..2nS，双组 nS+1..3nS）后解决。
2. **`active` 顺序必须与网表 V 元素顺序一致（最致命）**：8S2P 网表里 V 元素按"每级先 A 后 B"追加，顺序是 `[V0,V8,V1,V9,…]` 即电芯 `[0,8,1,9,…]`，但 `active` 一度错写成 `[0,1,2,…,15]`。结果 `set_topology` 把 cell1 的电压写进了本属于 cell8 的 `V8` 槽，整包 EMF 被错误"抹平"，**完全看不到环流**。修正后 `active_par = [0,8,1,9,2,10,…]` 与 V 元素顺序严格对齐。
3. **`Netlist` 无 `v_rows` 属性**：`set_topology` 原引用 `netlist.v_rows`（类里不存在），已改为按 `df["desc"]` 实时统计 V 元素数。
4. **异常分支返回值个数不一致**：热切换分支的 `except` 返回值个数与正常分支不匹配，已统一为 5 元组。

---

## 5. 文件改动一览

| 文件 | 改动 |
|------|------|
| `ecm_pack/pack.py` | `Pack.__init__` 增加 `active` 参数；新增 `set_topology()`；`solve()` 增加 `topology_events` / `switch_callback`；`_resolve_event()` 静态方法；输出新增 `Topology changes [s]`；断开电芯电流记 0、状态冻结 |
| `ecm_pack/circuit.py` | 新增 `setup_two_group(nS)` 工厂（连续节点编号 + 正确 `active` 顺序） |
| `ecm_pack/__init__.py` | 导出 `setup_two_group` |
| `ecm_pack/examples/topology_demo.py` | **新增**：8S→8S2P 热切换演示（含中文字体注册、四联图） |
| `ecm_pack/examples/demo.py` | 补充中文字体注册，避免图内中文成方块 |

---

## 6. 运行

```bash
cd /workspace
python3.11 ecm_pack/examples/topology_demo.py   # 拓扑热切换演示
python3.11 ecm_pack/examples/demo.py            # 原有四项能力演示
```
